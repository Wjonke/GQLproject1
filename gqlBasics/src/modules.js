const hello = 'Hello';
const name = 'Wes';
const location = 'Atlanta';
const getGreeting = (hello, name, location) => {
  return `${hello} ${name}, I see you are in ${location}, welcome to the course`;
};

export { hello, name, location, getGreeting };
